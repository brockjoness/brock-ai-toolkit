# Optional — only needed if you wire the router to call HeyGen or Veo 3 directly.
# By default the skill emits prompts and you submit them manually.

# TODO: set HEYGEN_API_KEY in .env
HEYGEN_API_KEY=

# TODO: set GOOGLE_VEO_API_KEY in .env (Veo 3, via Google's API)
GOOGLE_VEO_API_KEY=
